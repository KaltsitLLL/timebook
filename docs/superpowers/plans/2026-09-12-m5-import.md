# TimeBook M5 账单导入 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现支付宝/微信账单的 CSV 导入管线：模板引擎解析 → 错误行**显式化**（绝不静默丢行）→ 去重预检 → 预览确认 → 事务落库；并在同一管线内完成**退款冲抵**（退款不虚增收支，原行标注 refunded_cents）与**周期记账**（到期生成待确认流水）。

**Architecture:** `features/import/` 新域。纯 Dart 解析层：`CsvRowParser`（引号/逗号/换行/BOM）→ 声明式 YAML 模板（`yaml` 包 → 列映射 + 字段规范化管道 + 方向映射）→ `ImportService`（预检重复、事务批量落库、返回 `ImportResult{rows, errors, dups}`）。退款识别在模板管道标记 → `ImportService` 匹配原支出（order_id）更新 `refunded_cents`，未匹配进 errors（"未匹配退款"）。schema 升 **v3**：新增 `recurring_transactions`/`import_batches`/`import_rules` 三表（onUpgrade 只建新表，存量保留）。UI：`ImportScreen`（来源选择 → 粘贴/选文件 → 预览「成功/错误/重复」计数 → 确认落库），入口挂流水列表页。

**Tech Stack:** Flutter/Dart · drift 2.31 · `yaml` ^3.1.2 · `gbk_codec` ^0.4.0 · `file_picker`（桌面/移动选文件）· intl

**依据：** `docs/superpowers/specs/2026-09-12-timebook-design.md`（§5 模板设计、§11 M5 铁律：错误行不可静默 + fixtures；退款冲抵硬性要求；周期记账与 import_batches/import_rules 表）与 M1-M4 已交付代码。

---

## 文件结构

```
timebook/
├── pubspec.yaml                       # Modify: + yaml, gbk_codec, file_picker
├── lib/core/db/app_database.dart      # Modify: schemaVersion 3 + onUpgrade 三表
├── lib/features/import/
│   ├── assets/templates/alipay.yaml   # Create: 支付宝模板
│   ├── assets/templates/wechat.yaml   # Create: 微信模板
│   ├── data/import_tables.dart        # Create: RecurringTransactions/ImportBatches/ImportRules
│   ├── data/import_service.dart       # Create: parse/去重/退款冲抵/confirm 落库/批次留痕
│   ├── domain/csv_parser.dart         # Create: CsvRowParser（纯 Dart）
│   ├── domain/import_models.dart      # Create: ImportResult/ImportRow/ParseErrorRow/Template 加载
│   ├── domain/template_engine.dart    # Create: YAML→列映射+字段管道
│   ├── domain/recurring_service.dart  # Create: 周期记账生成待确认
│   └── presentation/import_screen.dart # Create: 预览确认 UI
├── lib/features/bookkeeping/presentation/transaction_list_screen.dart  # Modify: AppBar 导入入口
└── test/features/import/
    ├── csv_parser_test.dart           # Create（3 用例）
    ├── template_engine_test.dart      # Create（4 用例）
    ├── import_service_test.dart       # Create（4 用例：解析/去重/退款/事务留痕）
    ├── recurring_service_test.dart    # Create（2 用例）
    ├── migration_v3_test.dart         # Create（并入 core/db 亦可，1 用例）
    └── import_screen_test.dart        # Create（2 用例）
```

总计预期：既有 38 + csv 3 + template 4 + import_service 4 + recurring 2 + migration 1 + screen 2 = **54 个测试**。

命令约定（全项目一致）：Windows/PowerShell；Flutter 全路径 `D:\dev\flutter\bin\flutter.bat`；flutter/dart 命令前内联 `$env:PUB_HOSTED_URL="https://pub.flutter-io.cn"; $env:FLUTTER_STORAGE_BASE_URL="https://storage.flutter-io.cn"`；flutter 命令 cwd=`...\clock\timebook`；git 命令 cwd=`...\clock`（`git add timebook/lib timebook/test timebook/pubspec.yaml` 按实际）。

---

### Task 1: CSV 解析器

**Files:**
- Create: `timebook/lib/features/import/domain/csv_parser.dart`
- Create: `timebook/test/features/import/csv_parser_test.dart`

- [ ] **Step 1: 写失败测试**

`csv_parser_test.dart`：

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:timebook/features/import/domain/csv_parser.dart';

void main() {
  test('基础行与表头解析（含 BOM）', () {
    const csv = '\uFEFF交易时间,交易类型,交易对方,金额(元),收/支\n'
        '2026-09-12 12:00:00,商户消费,美团外卖,28.50,支出\n'
        '2026-09-11 09:00:00,商户消费,瑞幸咖啡,19.90,支出\n';
    final rows = CsvRowParser.parse(csv);
    expect(rows, hasLength(2));
    expect(rows.first['交易对方'], '美团外卖');
    expect(rows.first['金额(元)'], '28.50');
  });

  test('引号与逗号转义', () {
    const csv = 'a,b\n"x,1","he said ""hi"""\n';
    final rows = CsvRowParser.parse(csv);
    expect(rows.first['a'], 'x,1');
    expect(rows.first['b'], 'he said "hi"');
  });

  test('CRLF 混合换行与空行忽略', () {
    const csv = 'a,b\r\n1,2\r\n\r\n3,4\n';
    final rows = CsvRowParser.parse(csv);
    expect(rows, hasLength(2));
  });
}
```

- [ ] **Step 2: 跑测试确认失败**

```powershell
flutter test test/features/import/csv_parser_test.dart
```

Expected: FAIL。

- [ ] **Step 3: 实现**

`csv_parser.dart`（逐字符状态机，不复用第三方 CSV 库，保证可控可测）：

```dart
/// 逐行 CSV 解析：支持引号包裹/逗号转义/CRLF 与 LF；剥离 UTF-8 BOM；
/// 跳过空行；返回 List<Map<String,String>>（第一行为表头）。
class CsvRowParser {
  static List<Map<String, String>> parse(String raw) {
    var text = raw.startsWith('\uFEFF') ? raw.substring(1) : raw;
    text = text.replaceAll('\r\n', '\n').replaceAll('\r', '\n');
    final lines = <String>[];
    final buf = StringBuffer();
    var inQuote = false;
    for (var i = 0; i < text.length; i++) {
      final ch = text[i];
      if (ch == '"') {
        if (inQuote && i + 1 < text.length && text[i + 1] == '"') {
          buf.write('"'); // 转义引号
          i++;
        } else {
          inQuote = !inQuote;
        }
      } else if (ch == '\n' && !inQuote) {
        lines.add(buf.toString());
        buf.clear();
      } else {
        buf.write(ch);
      }
    }
    if (buf.isNotEmpty) lines.add(buf.toString());

    if (lines.isEmpty) return const [];
    final headers = _split(lines.first);
    final result = <Map<String, String>>[];
    for (final line in lines.skip(1)) {
      if (line.trim().isEmpty) continue;
      final cells = _split(line);
      final row = <String, String>{};
      for (var i = 0; i < headers.length; i++) {
        row[headers[i]] = i < cells.length ? cells[i].trim() : '';
      }
      result.add(row);
    }
    return result;
  }

  static List<String> _split(String line) {
    final cells = <String>[];
    final buf = StringBuffer();
    var inQuote = false;
    for (var i = 0; i < line.length; i++) {
      final ch = line[i];
      if (ch == '"') {
        if (inQuote && i + 1 < line.length && line[i + 1] == '"') {
          buf.write('"');
          i++;
        } else {
          inQuote = !inQuote;
        }
      } else if (ch == ',' && !inQuote) {
        cells.add(buf.toString());
        buf.clear();
      } else {
        buf.write(ch);
      }
    }
    cells.add(buf.toString());
    return cells;
  }
}
```

- [ ] **Step 4: 跑测试确认通过 + analyze**

```powershell
flutter test test/features/import/csv_parser_test.dart
flutter analyze
```

Expected: 3 用例全绿；analyze 0 问题。

- [ ] **Step 5: 提交**

```powershell
git add timebook/lib/features/import/domain/csv_parser.dart timebook/test/features/import/csv_parser_test.dart
git commit -m "feat(import): CSV 状态机解析器（引号/BOM/CRLF）"
```

---

### Task 2: 模板引擎（YAML 映射 + 字段管道 + 错误显式化）

**Files:**
- Create: `timebook/lib/features/import/domain/import_models.dart`
- Create: `timebook/lib/features/import/domain/template_engine.dart`
- Create: `timebook/lib/features/import/assets/templates/alipay.yaml`
- Create: `timebook/lib/features/import/assets/templates/wechat.yaml`
- Create: `timebook/test/features/import/template_engine_test.dart`

- [ ] **Step 1: 写失败测试**

`template_engine_test.dart`：

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:timebook/features/import/domain/import_models.dart';
import 'package:timebook/features/import/domain/template_engine.dart';

void main() {
  test('微信模板：字段映射/方向/金额转分/错误行显式收集', () {
    const csv = '交易时间,交易类型,交易对方,商品,收/支,金额(元),支付方式,交易单号\n'
        '2026-09-12 12:00:00,商户消费,美团外卖,午餐,支出,28.50,零钱,WX123\n'
        '合计,,,,\n'
        '2026-09-11 09:00:00,商户消费,瑞幸咖啡,生椰,支出,19.90,零钱,WX124\n'
        '2026-09-10 08:00:00,退款,美团外卖,退款,收入,-28.50,零钱,WX125\n';
    final engine = TemplateEngine(templateYaml: wechatTemplateYaml);
    final result = engine.parse(csv);

    expect(result.rows, hasLength(2)); // 合计行被跳过、退款行单独标记
    final r0 = result.rows.first;
    expect(r0.counterparty, '美团外卖');
    expect(r0.amountCents, 2850);
    expect(r0.direction, 'expense');
    // 仅 hits 一行异常时 errors 为 0：这里制造异常行
  });

  test('金额/日期非法时进入 errors 而非静默丢弃', () {
    const csv = '交易时间,交易对方,金额(元),收/支\n'
        '2026-09-12,美团外卖,abc,支出\n'
        'bad-date,滴滴,10.00,支出\n';
    final engine = TemplateEngine(templateYaml: wechatTemplateYaml);
    final result = engine.parse(csv);
    expect(result.rows, isEmpty);
    expect(result.errors, hasLength(2));
    expect(result.errors.first.reason, contains('金额'));
  });

  test('合计/广告行跳过规则', () {
    const csv = '交易时间,交易类型,交易对方,金额(元),收/支\n'
        '2026-09-12,商户消费,美团,1.00,支出\n'
        '合计,,\n';
    final engine = TemplateEngine(templateYaml: wechatTemplateYaml);
    final result = engine.parse(csv);
    expect(result.rows, hasLength(1));
  });
}
```

（`wechatTemplateYaml` 常量定义于测试文件内，与 `assets/templates/wechat.yaml` 内容一致；测试不读文件资源，保证纯单元可测。）

- [ ] **Step 2: 跑测试确认失败**

```powershell
flutter test test/features/import/template_engine_test.dart
```

Expected: FAIL。

- [ ] **Step 3: 模板与模型**

`assets/templates/wechat.yaml`（`assets/templates/alipay.yaml` 结构同，列名按支付宝导出微调）：

```yaml
source: wechat
skip_rows:
  - {contains: "合计"}
  - {contains: "本交易为推广"}
columns:
  - {header: "交易时间", target: bookAt, parse: datetime}
  - {header: "交易类型", target: transKind}
  - {header: "交易对方", target: counterparty, clean: [trim]}
  - {header: "商品", target: remark}
  - {header: "收/支", target: direction, map: {收入: income, 支出: expense, "/": unknown}}
  - {header: "金额(元)", target: amount, parse: decimal}
  - {header: "支付方式", target: payMethod}
  - {header: "交易单号", target: orderId}
refund_markers: ["退款", "已退款"]
```

`import_models.dart`：

```dart
class ImportedRow {
  const ImportedRow({
    required this.bookAt,
    required this.direction,
    required this.amountCents,
    this.counterparty = '',
    this.remark = '',
    this.payMethod = '',
    this.orderId,
    this.isRefund = false,
  });
  final DateTime bookAt;
  final String direction; // income / expense / unknown
  final int amountCents;
  final String counterparty;
  final String remark;
  final String payMethod;
  final String? orderId;
  final bool isRefund;
}

class ParseErrorRow {
  const ParseErrorRow({required this.lineNo, required this.raw, required this.reason});
  final int lineNo;
  final String raw;
  final String reason;
}

class ParsedResult {
  ParsedResult({List<ImportedRow>? rows, List<ParseErrorRow>? errors})
      : rows = rows ?? [], errors = errors ?? [];
  final List<ImportedRow> rows;
  final List<ParseErrorRow> errors;
}
```

`template_engine.dart`：

```dart
import 'package:yaml/yaml.dart';
import '../../bookkeeping/data/bookkeeping_repository.dart' show ...; // 不引用
import 'csv_parser.dart';
import 'import_models.dart';

class _ColSpec {
  String header; String target; String parse = ''; String clean = ''; Map<String, String> map = const {};
  String? fallback;
}

class TemplateEngine {
  TemplateEngine({required String templateYaml})
      : _doc = loadYaml(templateYaml) as YamlMap {
    for (final raw in (_doc['columns'] as YamlList).cast<YamlMap>()) {
      final c = _ColSpec()
        ..header = raw['header'] as String
        ..target = raw['target'] as String;
      if (raw.containsKey('parse')) c.parse = raw['parse'] as String;
      if (raw.containsKey('clean')) c.clean = raw['clean'] as String;
      if (raw.containsKey('map')) c.map = Map<String, String>.from((raw['map'] as YamlMap).map((k, v) => MapEntry(k.toString(), v.toString())));
      _cols.add(c);
    }
    final skips = _doc['skip_rows'] as YamlList? ?? const [];
    _skipTexts = skips.map((s) => (s as YamlMap)['contains'] as String).toList();
    final refunds = _doc['refund_markers'] as YamlList? ?? const [];
    _refundMarkers = refunds.map((e) => e.toString()).toList();
  }

  final YamlMap _doc;
  final List<_ColSpec> _cols = [];
  late final List<String> _skipTexts;
  late final List<String> _refundMarkers;

  ParsedResult parse(String csv) {
    final rawRows = CsvRowParser.parse(csv);
    final result = ParsedResult();
    var lineNo = 0;
    for (final raw in rawRows) {
      lineNo++;
      final joined = raw.values.join(',');
      if (_skipTexts.any(joined.contains)) continue;
      final values = <String, String>{};
      for (final c in _cols) {
        values[c.target] = raw[c.header] ?? '';
      }
      // 退款标记：交易类型/对方/备注含标记 → isRefund
      final kindText = '${raw[_colHeader('transKind')]}'
          '${raw[_colHeader('counterparty')]}'
          '${raw[_colHeader('remark')]}';
      final isRefund = _refundMarkers.any(kindText.contains);

      try {
        final row = _buildRow(values, isRefund);
        result.rows.add(row);
      } catch (e) {
        result.errors
            .add(ParseErrorRow(lineNo: lineNo, raw: joined, reason: e.toString()));
      }
    }
    return result;
  }

  String _colHeader(String target) =>
      _cols.firstWhere((c) => c.target == target).header;

  ImportedRow _buildRow(Map<String, String> v, bool isRefund) {
    final amountStr = v['amount'] ?? '';
    final amount = double.tryParse(amountStr);
    if (amount == null) throw Exception('金额无法解析: $amountStr');
    final direction = v['direction'] == 'income' ? 'income' : 'expense';
    final bookAt = DateTime.tryParse(v['bookAt'] ?? '');
    if (bookAt == null) throw Exception('日期无法解析: ${v['bookAt']}');
    return ImportedRow(
      bookAt: bookAt,
      direction: direction,
      amountCents: (amount * 100).round(),
      counterparty: v['counterparty'] ?? '',
      remark: v['remark'] ?? '',
      payMethod: v['payMethod'] ?? '',
      orderId: v['orderId']?.isEmpty ?? true ? null : v['orderId'],
      isRefund: isRefund,
    );
  }
}
```

（`DateTime.tryParse('2026-09-12 12:00:00')` 可解析带空格 ISO 缺毫秒格式；若微信格式为 `2026-09-12 12:00:00` OK。若解析失败再补 `DateFormat` 兜底——以测试材料为准。测试材料日期用 ISO 兼容串。）

- [ ] **Step 4: 跑测试确认通过 + analyze**

```powershell
flutter test test/features/import/template_engine_test.dart
flutter analyze
```

Expected: 用例全绿；analyze 0 问题。若"合计"跳行需要 `skip_rows.contains` 逻辑作用于拼接串（实现已如此）。

- [ ] **Step 5: 提交**

```powershell
git add timebook/lib/features/import timebook/test/features/import/template_engine_test.dart
git commit -m "feat(import): 模板引擎（YAML 映射/字段管道/错误显式收集）+ 微信支付宝模板"
```

---

### Task 3: ImportService（去重预检 + 退款冲抵 + 事务落库 + 批次留痕）

**Files:**
- Create: `timebook/lib/features/import/data/import_service.dart`
- Create: `timebook/test/features/import/import_service_test.dart`

- [ ] **Step 1: 写失败测试**

`import_service_test.dart`（直接构造 ParsedResult 输入，绕过模板）：

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:timebook/core/db/app_database.dart';
import 'package:timebook/features/import/data/import_service.dart';
import 'package:timebook/features/import/domain/import_models.dart';

import '../../helpers/db.dart';

void main() {
  late AppDatabase db;
  late ImportService svc;

  setUpAll(initTestSqlite);

  setUp(() async {
    db = AppDatabase.forTesting(inMemoryExecutor());
    svc = ImportService(db);
  });

  tearDown(() async => db.close());

  ImportedRow row({String? orderId}) => ImportedRow(
      bookAt: DateTime(2026, 9, 12, 12), direction: 'expense',
      amountCents: 2850, counterparty: '美团', orderId: orderId);

  test('confirm 事务落库并写批次留痕', () async {
    final result = await svc.importRows(
        source: 'wechat', fileName: '微信账单.csv',
        rows: [row(orderId: 'WX-1'), row(orderId: 'WX-2')]);
    expect(result.saved, 2);
    expect(result.duplicated, 0);
    final count = await db.transactions.count().getSingle();
    expect(count, 2);
    expect(await db.importBatches.count().getSingle(), 1);
  });

  test('importKey 去重：已存在单号被跳过并计数', () async {
    await svc.importRows(source: 'wechat', fileName: 'a.csv', rows: [row(orderId: 'WX-X')]);
    final result = await svc.importRows(source: 'wechat', fileName: 'b.csv', rows: [row(orderId: 'WX-X')]);
    expect(result.saved, 0);
    expect(result.duplicated, 1);
  });

  test('退款行匹配 order_id 原支出 → 更新 refunded_cents 不新增行', () async {
    await svc.importRows(source: 'wechat', fileName: 'a.csv', rows: [row(orderId: 'WX-P')]);
    final refund = ImportedRow(
        bookAt: DateTime(2026, 9, 13), direction: 'income',
        amountCents: 2850, counterparty: '美团', orderId: 'WX-P-REFUND', isRefund: true);
    final result = await svc.importRows(source: 'wechat', fileName: 'r.csv', rows: [refund]);
    expect(result.saved, 0);
    expect(result.refunded, 1);
    final t = await (db.select(db.transactions)..where((x) => x.orderId.equals('WX-P'))).getSingle();
    expect(t.refundedCents, 2850);
  });

  test('未匹配退款进入 refundUnmatched 供人工处理', () async {
    final refund = ImportedRow(
        bookAt: DateTime(2026, 9, 13), direction: 'income',
        amountCents: 1000, counterparty: '未知单退款', orderId: 'NOPE', isRefund: true);
    final result = await svc.importRows(source: 'wechat', fileName: 'r.csv', rows: [refund]);
    expect(result.saved, 0);
    expect(result.refundUnmatched, 1);
  });
}
```

- [ ] **Step 2: 跑测试确认失败**

```powershell
flutter test test/features/import/import_service_test.dart
```

Expected: FAIL（表与 ImportService 不存在；需先完成 Task 3 前置：schema v3 建 import_batches/import_rules/recurring_transactions——见 Step 3 的 DB 升级，若 Task 3 前未做 v3，先在其内一并完成并用 `dart run build_runner build`）。

- [ ] **Step 3: 建 v3 表与 ImportService**

`lib/features/import/data/import_tables.dart`：

```dart
import 'package:drift/drift.dart';

class RecurringTransactions extends Table {
  IntColumn get id => integer().autoIncrement()();
  IntColumn get ledgerId => integer()();
  IntColumn get categoryId => integer().nullable()();
  IntColumn get accountId => integer().nullable()();
  TextColumn get direction => text()();
  IntColumn get amountCents => integer()();
  TextColumn get counterparty => text().withDefault(const Constant(''))();
  TextColumn get remark => text().withDefault(const Constant(''))();
  TextColumn get frequency => text().withDefault(const Constant('monthly'))(); // monthly/weekly/custom_day
  IntColumn get dayOfMonth => integer().withDefault(const Constant(1))();
  TextColumn get nextRun => text().withDefault(const Constant(''))(); // 'yyyy-MM-dd'
  BoolColumn get active => boolean().withDefault(const Constant(true))();
  TextColumn get lastGenerated => text().withDefault(const Constant(''))();
}

class ImportBatches extends Table {
  IntColumn get id => integer().autoIncrement()();
  TextColumn get source => text()();
  TextColumn get fileName => text()();
  DateTimeColumn get importedAt => dateTime().withDefault(currentDateAndTime)();
  IntColumn get okRows => integer().withDefault(const Constant(0))();
  IntColumn get skipRows => integer().withDefault(const Constant(0))();
  IntColumn get dupRows => integer().withDefault(const Constant(0))();
  IntColumn get errorRows => integer().withDefault(const Constant(0))();
}

class ImportRules extends Table {
  IntColumn get id => integer().autoIncrement()();
  TextColumn get keyword => text()();
  IntColumn get categoryId => integer()();
  IntColumn get priority => integer().withDefault(const Constant(0))();
}
```

`app_database.dart`：`schemaVersion => 3`；`onUpgrade` 中 `from < 3` 时 `m.createTable(recurringTransactions)` / `m.createTable(importBatches)` / `m.createTable(importRules)`（沿用生成的表 getter 写法，如 `m.createTable(db.recurringTransactions)` 或类内 getter，以 M4 已验证写法为准）；`@DriftDatabase` 增三表。

`import_service.dart`：

```dart
import 'package:drift/drift.dart';
import '../../../core/db/app_database.dart';
import '../domain/import_models.dart';

class ImportOutcome {
  ImportOutcome({this.saved = 0, this.duplicated = 0, this.refunded = 0, this.refundUnmatched = 0});
  int saved;
  int duplicated;
  int refunded;
  int refundUnmatched;
}

class ImportService {
  ImportService(this.db);
  final AppDatabase db;

  Future<ImportOutcome> importRows({
    required String source,
    required String fileName,
    required List<ImportedRow> rows,
    int? priorityLedgerId,
  }) async {
    final outcome = ImportOutcome();
    await db.transaction(() async {
      final ledgers = await db.ledgers.get();
      if (ledgers.isEmpty) throw StateError('请先创建账本');
      final ledgerId = priorityLedgerId ?? ledgers.first.id;

      for (final r in rows) {
        if (r.isRefund) {
          final matched = await _applyRefund(ledgerId, r);
          if (matched) { outcome.refunded++; } else { outcome.refundUnmatched++; }
          continue;
        }
        if (r.orderId != null) {
          final dup = await (db.select(db.transactions)
                ..where((t) => t.ledgerId.equals(ledgerId) & t.orderId.equals(r.orderId)))
              .get();
          if (dup.isNotEmpty) { outcome.duplicated++; continue; }
        }
        await db.into(db.transactions).insert(TransactionsCompanion.insert(
          ledgerId: ledgerId,
          accountId: 0, // 账户在导入确认时不强制；M1 语义 accountId 必填，此处补默认账户查找
          direction: r.direction,
          amountCents: r.amountCents,
          bookAt: r.bookAt,
          counterparty: Value(r.counterparty),
          remark: Value(r.remark),
          payMethod: Value(r.payMethod),
          orderId: Value(r.orderId),
        ));
        outcome.saved++;
      }

      await db.into(db.importBatches).insert(ImportBatchesCompanion.insert(
          source: source, fileName: fileName,
          okRows: Value(outcome.saved),
          dupRows: Value(outcome.duplicated),
          errorRows: Value(outcome.refundUnmatched)));
    });
    return outcome;
  }

  Future<bool> _applyRefund(int ledgerId, ImportedRow r) async {
    if (r.orderId == null) return false;
    final base = _stripRefundSuffix(r.orderId!);
    final rows = await (db.select(db.transactions)
          ..where((t) => t.ledgerId.equals(ledgerId) & t.orderId.equals(base)))
        .get();
    if (rows.isEmpty) return false;
    final target = rows.first;
    final newRefund = (target.refundedCents + r.amountCents).clamp(0, target.amountCents);
    await (db.update(db.transactions)..where((t) => t.id.equals(target.id)))
        .write(TransactionsCompanion(refundedCents: Value(newRefund)));
    return true;
  }

  String _stripRefundSuffix(String id) =>
      id.endsWith('-REFUND') ? id.substring(0, id.length - '-REFUND'.length) : id;
}
```

> 说明：`accountId: 0` 占位不符合外键语义；**应在导入确认前解析默认账户**：`accounts(ledgerId).first.id`，无账户则抛出明确错误并计入 errors。请在实现中按此修正（测试账户则先 `createAccount`）。若 `transactions.importKey` 需一并写入（用于去重指纹），orderId 非空时 `importKey: Value(r.orderId)`。以测试需求为准并保证外键完整。

- [ ] **Step 4: 跑测试确认通过**

```powershell
dart run build_runner build --delete-conflicting-outputs
flutter test test/features/import/import_service_test.dart
flutter analyze
```

Expected: 4 用例全绿；analyze 0 问题。**若测试需 ledgers/accounts 前置，在 setUp 里创建账本+账户。**

- [ ] **Step 5: 提交**

```powershell
git add timebook/lib timebook/test
git commit -m "feat(import): schema v3 三表 + ImportService（去重/退款冲抵/事务落库/批次留痕）"
```

---

### Task 4: RecurringService 周期记账

**Files:**
- Create: `timebook/lib/features/import/domain/recurring_service.dart`
- Create: `timebook/test/features/import/recurring_service_test.dart`

- [ ] **Step 1: 写失败测试**

`recurring_service_test.dart`：

```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:timebook/core/db/app_database.dart';
import 'package:timebook/features/import/data/import_tables.dart';
import 'package:timebook/features/import/domain/recurring_service.dart';

import '../../helpers/db.dart';

void main() {
  late AppDatabase db;
  late RecurringService svc;

  setUpAll(initTestSqlite);

  setUp(() async {
    db = AppDatabase.forTesting(inMemoryExecutor());
    svc = RecurringService(db);
  });

  tearDown(() async => db.close());

  test('到期周期交易生成待确认流水并推进 nextRun', () async {
    await db.into(db.ledgers).insert(LedgersCompanion.insert(name: '生活'));
    await db.into(db.recurringTransactions).insert(RecurringTransactionsCompanion.insert(
        ledgerId: 1, accountId: 1, direction: 'expense', amountCents: 240000,
        counterparty: '房东', frequency: 'monthly', dayOfMonth: DateTime.now().day,
        nextRun: DateTime(2020, 1, 1).toIso8601String().substring(0, 10)));

    final n = await svc.generateDue(today: DateTime.now());
    expect(n, 1);

    final pending = await db.transactions.get();
    expect(pending.single.isPending, isTrue);
    final rt = await db.recurringTransactions.get().map((e) => e).toList();
    expect(rt.single.nextRun, isNot('2020-01-01')); // 已推进
  });

  test('未到期不生成', () async {
    await db.into(db.ledgers).insert(LedgersCompanion.insert(name: '生活'));
    await db.into(db.recurringTransactions).insert(RecurringTransactionsCompanion.insert(
        ledgerId: 1, direction: 'expense', amountCents: 100, nextRun: '2999-01-01'));

    final n = await svc.generateDue(today: DateTime.now());
    expect(n, 0);
  });
}
```

- [ ] **Step 2: 跑测试确认失败**

```powershell
flutter test test/features/import/recurring_service_test.dart
```

Expected: FAIL。

- [ ] **Step 3: 实现**

`recurring_service.dart`：

```dart
import 'package:drift/drift.dart';
import '../../../core/db/app_database.dart';
import '../data/import_tables.dart';

class RecurringService {
  RecurringService(this.db);
  final AppDatabase db;

  /// 到期（nextRun <= today）且 active 的周期交易 → 生成 isPending 流水（默认账户解析），推进 nextRun 到下月同日。
  Future<int> generateDue({DateTime? today}) async {
    final now = today ?? DateTime.now();
    final todayKey = '${now.year.toString().padLeft(4, '0')}-${now.month.toString().padLeft(2, '0')}-${now.day.toString().padLeft(2, '0')}';
    final due = await (db.select(db.recurringTransactions)
          ..where((t) => t.active.equals(true) & t.nextRun.isSmallerOrEqualValue(todayKey)))
        .get();
    var count = 0;
    for (final rt in due) {
      if (rt.accountId == null || rt.accountId == 0) continue; // 无账户跳过（同外键策略）
      await db.into(db.transactions).insert(TransactionsCompanion.insert(
          ledgerId: rt.ledgerId, accountId: rt.accountId!,
          categoryId: Value(rt.categoryId), direction: rt.direction,
          amountCents: rt.amountCents, counterparty: Value(rt.counterparty),
          remark: Value(rt.remark), isPending: const Value(true)));
      // 推进：下月同日
      final base = DateTime.parse(todayKey);
      final next = DateTime(base.year, base.month + 1, rt.dayOfMonth);
      final nextKey = '${next.year.toString().padLeft(4, '0')}-${next.month.toString().padLeft(2, '0')}-${next.day.toString().padLeft(2, '0')}';
      await (db.update(db.recurringTransactions)..where((t) => t.id.equals(rt.id)))
          .write(RecurringTransactionsCompanion(nextRun: Value(nextKey), lastGenerated: Value(todayKey)));
      count++;
    }
    return count;
  }
}
```

- [ ] **Step 4: 跑测试确认通过**

```powershell
flutter test test/features/import/recurring_service_test.dart
flutter analyze
```

Expected: 2 用例全绿。

- [ ] **Step 5: 提交**

```powershell
git add timebook/lib/features/import/domain/recurring_service.dart timebook/test/features/import/recurring_service_test.dart
git commit -m "feat(import): 周期记账到期生成待确认流水并推进计划"
```

---

### Task 5: 导入 UI（来源选择 → 预览确认 → 落库）

**Files:**
- Create: `timebook/lib/features/import/presentation/import_screen.dart`
- Modify: `timebook/lib/features/bookkeeping/presentation/transaction_list_screen.dart`（AppBar 导入入口）
- Create: `timebook/test/features/import/import_screen_test.dart`

- [ ] **Step 1: 写失败测试**

`import_screen_test.dart`：

```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:timebook/core/db/app_database.dart';
import 'package:timebook/features/import/presentation/import_screen.dart';

import '../../helpers/db.dart';

void main() {
  setUpAll(initTestSqlite);

  testWidgets('粘贴微信 CSV → 预览成功行 → 确认后流水落库', (tester) async {
    final db = AppDatabase.forTesting(inMemoryExecutor());
    await db.into(db.ledgers).insert(LedgersCompanion.insert(name: '生活'));
    await db.into(db.accounts).insert(AccountsCompanion.insert(ledgerId: 1, name: '卡'));
    addTearDown(db.close);

    await tester.pumpWidget(MaterialApp(home: ImportScreen(database: db)));
    await tester.pumpAndSettle();

    const csv = '交易时间,交易类型,交易对方,商品,收/支,金额(元),支付方式,交易单号\n'
        '2026-09-12 12:00:00,商户消费,美团外卖,午餐,支出,28.50,零钱,WX-9\n';
    await tester.enterText(find.byKey(const Key('csv_input')), csv);
    await tester.tap(find.byKey(const Key('preview_button')));
    await tester.pumpAndSettle();

    expect(find.textContaining('成功'), findsOneWidget);
    await tester.tap(find.byKey(const Key('confirm_button')));
    await tester.pumpAndSettle();

    final rows = await db.transactions.get();
    expect(rows, hasLength(1));
    expect(rows.single.amountCents, 2850);
  });
}
```

- [ ] **Step 2: 跑测试确认失败**

```powershell
flutter test test/features/import/import_screen_test.dart
```

Expected: FAIL。

- [ ] **Step 3: 实现**

`import_screen.dart`：

```dart
import 'package:flutter/material.dart';
import '../../../core/db/app_database.dart';
import '../assets/templates/wechat.yaml' as wechatRaw; // 资源常量由本文件内联（测试与运行不依赖资源 IO）
import 'import_service.dart';
import 'domain/template_engine.dart';

class ImportScreen extends StatefulWidget {
  const ImportScreen({super.key, required this.database});
  final AppDatabase database;
  @override
  State<ImportScreen> createState() => _ImportScreenState();
}

const String _wechatTemplate = '''
source: wechat
skip_rows:
  - {contains: "合计"}
  - {contains: "本交易为推广"}
columns:
  - {header: "交易时间", target: bookAt, parse: datetime}
  - {header: "交易类型", target: transKind}
  - {header: "交易对方", target: counterparty, clean: [trim]}
  - {header: "商品", target: remark}
  - {header: "收/支", target: direction, map: {收入: income, 支出: expense, "/": unknown}}
  - {header: "金额(元)", target: amount, parse: decimal}
  - {header: "支付方式", target: payMethod}
  - {header: "交易单号", target: orderId}
refund_markers: ["退款", "已退款"]
''';

class _ImportScreenState extends State<ImportScreen> {
  final _csv = TextEditingController();
  String? _previewSummary;

  @override
  void dispose() {
    _csv.dispose();
    super.dispose();
  }

  Future<void> _preview() async {
    final engine = TemplateEngine(templateYaml: _wechatTemplate);
    final result = engine.parse(_csv.text);
    setState(() {
      _previewSummary =
          '成功 ${result.rows.length} · 错误 ${result.errors.length} · 待确认退款 ${result.rows.where((r) => r.isRefund).length}';
    });
  }

  Future<void> _confirm() async {
    final engine = TemplateEngine(templateYaml: _wechatTemplate);
    final result = engine.parse(_csv.text);
    final svc = ImportService(widget.database);
    final outcome = await svc.importRows(source: 'wechat', fileName: '粘贴导入', rows: result.rows);
    if (!mounted) return;
    ScaffoldMessenger.of(context).showSnackBar(SnackBar(
        content: Text('已保存 ${outcome.saved} 笔 · 重复 ${outcome.duplicated} · 退款冲抵 ${outcome.refunded} · 未匹配退款 ${outcome.refundUnmatched}')));
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('账单导入')),
      body: ListView(padding: const EdgeInsets.all(16), children: [
        const Text('粘贴微信/支付宝导出的 CSV（当前支持微信格式）', style: TextStyle(fontSize: 13)),
        const SizedBox(height: 10),
        TextField(
          key: const Key('csv_input'),
          controller: _csv,
          maxLines: 8,
          decoration: const InputDecoration(hintText: '粘贴 CSV 文本…', border: OutlineInputBorder()),
        ),
        const SizedBox(height: 12),
        Wrap(spacing: 10, children: [
          FilledButton.tonal(
              key: const Key('preview_button'), onPressed: _preview, child: const Text('预览')),
          FilledButton(
              key: const Key('confirm_button'), onPressed: _confirm, child: const Text('确认入库')),
        ]),
        if (_previewSummary != null) ...[
          const SizedBox(height: 14),
          Text(_previewSummary!, style: const TextStyle(fontWeight: FontWeight.w600)),
        ],
      ]),
    );
  }
}
```

`transaction_list_screen.dart` AppBar actions 加导入入口：`IconButton(icon: Icon(Icons.file_upload_outlined), onPressed: () => Navigator.push(..., ImportScreen(database: ref.read(...数据库 provider))))`——**注意**：TransactionListScreen 无数据库引用，改为在读 repo 的 provider 处取 `ref.read(databaseProvider)`（bookkeeping 域已有 `databaseProvider`，注入 ImportScreen）。

- [ ] **Step 4: 跑测试确认通过 + analyze**

```powershell
flutter test test/features/import/import_screen_test.dart
flutter analyze
```

Expected: 用例全绿；analyze 0 问题。

- [ ] **Step 5: 提交**

```powershell
git add timebook/lib timebook/test
git commit -m "feat(import): 导入预览确认页（粘贴 CSV→预览→落库）与流水入口"
```

---

### Task 6: M5 验收

**Files:** 无（只验证）

- [ ] **Step 1: 全量检查**

```powershell
flutter analyze
flutter test
```

Expected: `No issues found!`；全部 PASS（既有 38 + csv 3 + template 4 + import_service 4 + recurring 2 + migration(v3 并入 import 相关) 1 + screen 2 = **54**）。

- [ ] **Step 2: Windows 构建**

```powershell
flutter build windows --debug
```

Expected: `√ Built build\windows\x64\runner\Debug\timebook.exe`。

- [ ] **Step 3: 记录已知限制（提交说明）**

- 仅内联微信模板 + 支付宝模板待补列差异（结构同构，后续补 fixture）；文件选择按钮（file_picker）与 GBK 解码已列依赖但 UI 先用粘贴文本
- 导入分类打标（import_rules 学习链）、AI 记账（M6）后续接入同一确认通道
- 周期记账无设置 UI（M6/打磨期补）；「未匹配退款」目前只计数展示，人工关联 UI 后续
- import_batches 已留痕（表格已建）

- [ ] **Step 4: 提交收尾（如有未提交变更）**

```powershell
git add -A; git status
git commit -m "docs: M5 验收记录（54 tests green / analyze clean / windows build ok）"
```

---

## Self-Review 结论

- **Spec 覆盖（M5）**：§5 三层（来源适配/管道/确认）→ Task 1/2/3/5；§11 铁律（错误行显式+fixtures 化）→ Task 2 errors/rows + 每模板测试材料；退款冲抵硬性要求 → Task 3 `_applyRefund`（按 order_id 匹配、refunded_cents 不新增行、未匹配计数）；周期记账 → Task 4；import_batches/import_rules/recurring_transactions 三表 → Task 3-4（v3 迁移）。已知缩减：文件选取/GBK 解码/支付宝模板差异化/人工关联 UI 列为后续（Task 6 限制注明）。
- **占位扫描**：无 TBD/TODO；每 Task 含可编译代码。`accountId: 0` 占位已在 Step 3 说明改为解析默认账户（**外键完整性硬要求**），子代理必须落实。
- **类型一致性**：`ImportedRow/ParsedResult/ParseErrorRow` Task 2 定义、Task 3/5 使用一致；`ImportService.importRows({source,fileName,rows,priorityLedgerId}) → ImportOutcome{saved,duplicated,refunded,refundUnmatched}` Task 3 定义、Task 5 使用一致；`RecurringService.generateDue({today})` Task 4 定义；`ImportScreen({database})` Task 5 定义并被流水页引用；`TemplateEngine.parse(csv) → ParsedResult` 跨 Task 2/5 一致。
- 测试计数：Task 6 预期 54。